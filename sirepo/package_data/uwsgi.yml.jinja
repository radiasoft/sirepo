---
# -*-yaml-*-
uwsgi:
  socket: :{{ port }}
  master: true
  processes: 1
  threads: 40
  wsgi-file: {{ uwsgi_py }}
  callable: app
  chdir = {{ db_dir }}
  die-on-term: true
  stats: {{ db_dir }}/sock
  pidfile: {{ db_dir }}/pid
  {% if docker %}
  logto = {{ db_dir }}/uwsgi.log
  {% endif %}
